---
_organized: true
---
# Продукт, команда, обмеження

## Продукт
Precoro: procurement SaaS. Документообіг PR → RFP → PO → Receipt → Invoice → Payment, плюс WR, Expense, Contract, бюджети, approval workflow, кастомні поля трьох типів (DCF документ, ICF позиція, SCF постачальник), інтеграції (NetSuite, QBO, Xero, punchout Amazon/FisherSci, OCR). Матриця доступів і мільйони комбінацій налаштувань. Ніхто не знає всіх взаємозв'язків, і це нормально.

90% прод-багів пов'язані з комбінацією налаштувань, а не з базовим флоу. Тест-кейси історично писались від функціоналу, а покриття комбінацій додавалось реактивно після прод-багів.

## UI змінюється часто
Фронтенд на Vue (`data-v-app`, кореневі елементи на кшталт `#PurchaseRequisitionIndexApp`). `data-test-id` у продукті є: на сторінці списку PR їх 256 (перевірено 2026-09-21). Дві конвенції імен одночасно: `breadcrumbs.link_dashboard` (область.тип_назва) і `button:save`, `toast:success` (тип:назва). Покриття не повне: там, де test-id нема, TAF іде XPath по тексту або структурі. Глобальні накладки мають стабільні id: `#sessionExpirePopup`, `#switchedCompanyPopup`, `#SwitchToMobileAppBanner`, `.notification-bar--outdated-browser`.
Редизайни і зміни інтерфейсу відбуваються регулярно; в Asana є модулі "New Design" і "Front". Один popup уже клав усі тести (P6). Відповідь фреймворку на це: `04-target/page-layer.md`, розділ "Редизайни".

## Середовища
| Назва | Що це |
|---|---|
| COM | прод США |
| US | прод ЄС |
| Precorino | тестове середовище, куди зливається розробка; сюди раз на 2–4 тижні заливається анонімізований дамп проду |
| pre_dev / гілкові | середовища під гілку девелопера; піднімаються без accounts-мікросервісу, тому компанії там створювати не можна |
| docker | локальний стек |

Sentry на DigitalOcean дивиться на COM, US і Precorino.

## Дані
Дві БД: моноліт і мікросервіс акаунтів/юзерів, мають бути синхронізовані. Кількість мікросервісів росте. Тестові дані це курований шматок анонімізованого дампу, який тримають живим міграціями. Від сідінгу бази перед прогоном відмовились: складно підтримувати, бо продукт постійно змінює схему.

## Команда
- Head of QA: мануальна частина, ієрархія команди.
- Yevhen: напрямок автоматизації, технічні рішення, TAF.
- AQA 1 (Максим Бринза): сильніший в інфраструктурі (Docker, Jenkins, Kubernetes, репортинг, дашборди). Основний контрібʼютор в CI.
- AQA 2: більш сфокусований на тестах.
- Дмитро Ясмо: performance-інженер (нещодавно), аналіз продуктивності проду і деву, покращення. Контакт для питань потужностей, php-fpm, Grafana, нічних зависань.
- General QA: feature owners по модулях; мають самі вирішувати, які автотести дописувати, і писати їх. TAF має бути придатним для цього та для постійного розширення.

Unit-тестів на продукті нема (або мало).

## Люди і ролі поза QA
- Марк: CTO. Автоматизація йому болить, і через недовіру він втручається в процеси QA мікроменеджментом; результати, що повертають довіру, це головна ціль. Приймає рішення про повні прогони на гілках, заборонив ручні зелені білди, запропонував вимкнення тестів через Qase. Питає "коли" і "гарантуєш?". Погодився на 4 години прогону за умови стабільного результату.
- Олександр Козлов: AQA команди документів, ревʼюїть тести General QA, знайшов різницю конфігурації нод на гілках.

## Комунікація
Уся комунікація лише через офіційні канали: Asana і відкриті Slack-канали (Jenkins Failed Builds для гілкових прогонів). Історія проблем читається там.

## Політики керівництва
- Мета: повний прогін на кожній гілці девелопера як quality gate. Причина: регресії на проді; замість unit-тестів вирішили використати E2E, бо вони є.
- Довжина прогону не є болем: 4 години прийнятні за умови стабільного і зрозумілого результату.
- Гілка прив'язана до задачі в Asana; поле модуля визначає smoke + модуль або повний прогін.
- Очікування: 100% зелених; одне падіння → розбір, і часто повний перезапуск на 3+ години.
- Ретраї зараз 1 (гілки) або 3 (Linux-конфіг у release).
