---
_organized: true
_favorite: true
_favorite_index: 4
_width: wide
---
# Метрики

Baseline: аудит `release` 2026-09-01 і нічний прогін 2026-09-16 (`01-context/nightly-run-2026-09-16.md`, коміт `d4927c3`, Windows-агент, 9 воркерів, 1 ретрай). Оновлювати при закритті кожної задачі.

## Прогін

| Метрика | Baseline | Фаза 1 | Фаза 2 | Фаза 3 |
| --- | --- | --- | --- | --- |
| Gate на PR | немає | ≤ 15 хв | ≤ 10 хв | ≤ 10 хв |
| Повний прогін wall-clock (9 воркерів) | 3h28m | ≤ 60 хв | ≤ 45 хв | ≤ 40 хв |
| Machine-time | 28h20m | ≤ 17h | ≤ 14h | ≤ 12h |
| Найдовший проект | 2h26m (minor\_company) | ≤ 45 хв | ≤ 30 хв | ≤ 30 хв |
| Flaky rate | 1.48% (32 / 2 158) | ≤ 1% | ≤ 0.5% | ≤ 0.3% |
| Retries | 1 / 3 | 1 | 1 | 1 |
| Timeout за замовчуванням | 500 s | 120 s | 120 s | 120 s |
| Перезапуск після падіння | повний | `--last-failed` |  |  |

## Контрольні показники коду

Можуть лише зменшуватись. Спосіб: **codemod** без прогону; **codemod + local run** порціями з підтвердженням зачеплених тестів локально; **instrument + nightly run** спершу інструментований fallback із логом, потім один повний нічний прогін і рішення по кластерах, бо кожен випадок може прикривати поведінку продукту.

| Показник | Baseline | Ціль | Спосіб |
| --- | --- | --- | --- |
| `allure.` у `src/ui` | ~12 000 | 0 | codemod |
| `expect(await` у спеках | 511 | 0 | codemod |
| Value-assertion на локаторах | ~5 000 | ≤ 500 | codemod + local run |
| `waitForTimeout` + `setTimeout` sleeps | 121 | 0 | instrument + nightly run |
| `.catch(() => false)` поза предикатами | ~211 | 0 | instrument + nightly run |
| `.catch(() => {})` | 131 | 0 | instrument + nightly run |
| `force`/`clickUsingJavascript`/`dispatchEvent` | ~420 | ≤ 20, кожен з коментарем | instrument + nightly run |
| `doLogin`/`loginAs` у спеках | 498 | 0 | codemod |
| `db.` у спеках | 1 811 | 0 | codemod + local run |
| `Constants.` | 791 | 0 | codemod |
| Локальні `mergeTests` у спеках | 37 | 0 | codemod |
| `xpath=` у локаторах | 1 047 | ≤ 100 | codemod + local run |
| `this.xxxLocators.` | 1 814 | 0 | codemod |
| Імпорти `precoro_service` | усі | 0 | codemod + local run |
| Spec-файлів > 1 500 рядків | 27 | 0 | codemod + local run |
| Не-spec файлів > 1 000 рядків | 10 | 0 | codemod + local run |
| INI-файлів | 7 | 0 | codemod |
| `tsc` помилок / `eslint` помилок | 3 / 109 | 0 / 0 | codemod + local run |
| Тестів `@not_for_isolated_env` (не біжать на гілках) | 1 087 (42% прогону, 12h) | 0 | instrument + nightly run |
| Секретів у репозиторії | 3 типи | 0 | codemod |
| Swagger-ендпоінтів у клієнті | ~177/507 | 100% використаних | codemod |

Як збираються показники і метрики прогону: `06-playbooks/measurement.md`.

### Де видно показники боргу
| Де | Що | Для кого |
|---|---|---|
| Термінал, `npm run debt` | таблиця "показник, develop, гілка, Δ" з файлом і рядком для кожного зростання | той, хто пише код |
| PR у TAF | перевірка в checks і коментар бота з рядками, що змінились; при зменшенні "baseline оновлено" | автор і ревʼюер |
| Репозиторій | `debt-baseline.json` (правда на `develop`), `metrics/debt.jsonl` (рядок на мерж) | історія, дашборд |
| `metrics/weekly/YYYY-Www.md`, щопонеділка | дельти за тиждень, PR-атрибуція, Mermaid-графік індексу боргу від 1.0 вниз | Yevhen, Head of QA, звіт керівництву |

Індекс боргу: кожен показник поділений на своє стартове значення, усереднено; один рядок замість двадцяти.

## Статус вимірювань

Принцип: метрика існує, лише якщо від неї залежить рішення.

### Рівень 1. Доступно з коду

| Метрика | Яке рішення підтримує | Статус на 2026-09-21 | Де цифра |
| --- | --- | --- | --- |
| Контрольні показники (~15 патернів) | Порядок codemod-ів; щотижневий прогрес | Виміряно вручну 15.09 на `release`; скрипт і CI-гейт ще не в репозиторії (T0-13) | `metrics.md`, показники коду |
| Помилки `tsc` / `eslint` | Чи можна вмикати гейт | Виміряно: tsc 3 (2 після правок), eslint 109 із розкладом за правилами | картки T0-01, T0-02 |
| Тестів на проект і на файл, теги | Розбиття проектів, tiering | Виміряно: 2 775 на `release`, 2 799 на робочій гілці; по проектах з прогону 16.09 | `taf-as-is.md`, `nightly-run-2026-09-16.md` |
| Профіль preparation | Черга API-preparation | Виміряно двічі: статично (310 UI-створень, 802 UI-confirm, 498 логінів проти 348 API) і за кроками прогону (підготовка 67%, перевірки 9%) | `archive/audit-2026-09-15.md`, `nightly-run-2026-09-16.md` |
| Покриття Swagger клієнтом | Обсяг T1-07 і генерації | Виміряно: ~177 із 507; Swagger ще росте, перерахувати після 18.09 | `archive/audit-2026-09-15.md` §9 |
| Колізії юзерів і проектів | Ризики паралелізму | Виміряно: main/afaxys один юзер, 4 спеки пишуть у чужу компанію | `taf-as-is.md` |
| З'єднання до MySQL у піку | Чи TAF співучасник потужностей | Оцінено статично (до ~108); рантайм-заміру нема; після відповіді про php-fpm (E2) пріоритет низький | `ci-and-envs.md` |

### Рівень 2. Дані від Макса і з Jenkins

| Що просили | Статус | Де цифра |
| --- | --- | --- |
| `latest.json` і `history.jsonl` за ~15 білдів | Отримано один прогін (16.09). Історії немає; тренд flaky 1–3% відомий лише зі скріншота дашборда | `nightly-run-2026-09-16.md` |
| Архів Allure одного нічного прогону | Отримано, розібрано: тривалості, кроки, категорії падінь, timeline воркерів | `nightly-run-2026-09-16.md`, `metrics.md` |
| Конфіг нічної джоби (Jenkinsfile) | Не отримано. Склад прогону з'ясовано зі слів: email-тести і `@heavy` окремо, OCR і NetSuite поза нічним, `@not_for_isolated_env` не біжить на гілках | питання #3 закрито; Jenkinsfile лишається потрібним для T0-06, T0-17, T0-23 |
| Доступ до Grafana | Не отримано; відомо, що удень OOM php-fpm, уночі аномалій навантаження нема | `ci-and-envs.md` E1, E2 |
| Експорт каналу Jenkins Failed Builds | Отримано без тредів; розібрано: 686 фейлів, категорії стадій, перезапуски | `jenkins-failed-builds.md` |
| Список модулів Asana | Отримано, 112 модулів | `modules.md` |

### Рівень 3. Потребує патча і прогону

| Метрика | Статус | Задача |
| --- | --- | --- |
| Запити на тест, браузер і API окремо | Не почато | T0-15 |
| `perFile`, `perTag` у репортері | Частково на гілці (`metrics-reporter`), полів ще нема | T0-04 |
| Мова прогону в метриках | Не почато | T0-07 |
| Статистика гілкових прогонів | Не почато; репортер на гілках вимкнений | T0-23 |
| Категорії падінь на рівні тредів | Не почато; на рівні стадій і на рівні одного прогону зроблено | T0-18 |

### Рівень 4. Потребує часу

Тренди після змін. Baseline-точка одна (прогін 16.09, коміт `d4927c3`); історія по білдах досі в інфра-AQA.

### Що ще не виміряно і чому це важливо

1. Історія нічних прогонів по білдах: без неї не відрізнити шум одного прогону від тренду. Одне прохання до інфра-AQA.
2. Частка червоних гілкових прогонів і перезапуски на PR: узагалі нема даних (P16). T0-23.
3. Запити на тест: єдина метрика навантаження, незалежна від завантаженості Precorino. T0-15, один патч і один прогін.
4. Категорії падінь із тредів: "фейл чи флейк" очима тих, хто розбирав. T0-18, експорт із тредами або читання каналу.

### Мінімальний набір, щоб почати модифікацію

1. Показники боргу: є цифри, нема скрипта і гейта → T0-13 перша задача коду.
2. Machine-time і flaky по проектах: є з одного прогону → достатньо для старту.
3. Top-50 і частка preparation: є → достатньо.
4. Запити на тест: нема → до пілота API-preparation (T1-09), не до старту.

Висновок: вимірювального блокера для старту етапу 1 немає. Бракує лише скрипта показників у репозиторії, і це сама перша задача.

### Правила порівняння

- Тривалість на спільному Precorino шумна: брати медіану 2–3 нічних прогонів, не один.
- Кількість запитів на тест стабільна: використовувати її як основний приймальний критерій задач API-preparation, тривалість як вторинний.
- Порівнювати лише однакові множини: після відповіді на питання #3 зафіксувати, які проекти і теги входять у нічний прогін.
- Кожна цифра baseline підписана комітом, номером білда, агентом, середовищем і мовою.

### Чого свідомо не міряємо

Покриття продукту тестами у відсотках, кількість рядків коду, кількість assertion, кількість тест-кейсів у Qase. Жодна з цих цифр не змінює рішення в цьому плані і створює хибне відчуття прогресу.
