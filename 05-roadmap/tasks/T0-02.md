---
_organized: true
---
# T0-02. Зелений `eslint` без ослаблення правил
Біль: P12.

## Контекст
109 помилок: `eqeqeq` 77, `no-floating-promises` 20, `prefer-const` 6, `no-unused-vars` 3, `no-empty-object-type` 1, `prefer-web-first-assertions` 1. Автофіксом закривається лише 7. Половина в `test_billing.spec.ts` (50) і `test_reports.spec.ts` (22). Конфіг: `eslint.config.js`, type-aware правила навмисно вимкнені, окрім `no-floating-promises`.

## Підхід
1. `npm run lint:fix` для 7 автофіксів, окремий коміт.
2. `eqeqeq` 77: codemod `==` → `===` через ts-morph з ручним переглядом порівнянь із `null`/`undefined` (там `== null` буває навмисним; замінити на явне `=== null || === undefined` або лишити з коментарем). Файлами: billing, reports, решта.
3. `no-floating-promises` 20: кожен випадок це або забутий `await` (баг, який ховає падіння), або навмисний fire-and-forget; перше чинити, друге позначати `void` з коментарем.
4. Решта 11 руками.
5. Крок `npm run lint` у Jenkins, червоний при помилці. Warnings (54) не чіпати зараз.
6. Не вмикати нових правил у цій задачі; нові правила вмикаються по мірі codemod-ів (`06-playbooks/playbook-codemod-metrics.md`).

## Готово, коли
0 помилок; крок у CI; кількість warnings не зросла.

## Ризики
`no-floating-promises` фікси змінюють поведінку тестів: забутий `await` міг ховати падіння. Прогнати зачеплені спеки на Precorino до мержу.

Оцінка: 1–2 дні.
