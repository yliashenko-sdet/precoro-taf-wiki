---
_organized: true
---
# T0-03. Секрети поза репозиторієм
Біль: P12. Статус: todo; етап 3 blocked (Максим).

## Контекст
Аудит `release` (2026-09-01) знайшов 3 секрети в репо: basic-auth MailPit у всіх 7 INI, живий токен `/create/password/<token>` проду в `configuration_app.ini:237`, хардкоджені креденшели `ControlClient`.

Замір 2026-09-25 на `develop` (`ad66840`): команда вже переробила конфіг (Олександр, "improve run env", 13–14.09). INI лишився один (`configuration.ini`), `configuration_app.ini` видалено разом із прод-токеном. `MAILPIT_URL` читається лише з env, без fallback, з поясненням у коді (`requireMailPitUrl`). У робочому дереві лишилось 2 секрети:
1. `mail_pit_quick_emails_url` у `configuration.ini:9` з basic-auth; читається як `process.env.MAILPIT_QUICK_EMAILS_URL ?? INI` (`read_configs.ts:421`), тобто env уже має пріоритет.
2. `ControlClient`: логін і пароль basic-auth константами в `src/api/control_service.ts:18-19` і в JSDoc на рядку 13; env не читається.

Прод-токен і креденшели лишаються в git-історії.

## Підхід
Етап 1, локально, без зміни поведінки:
1. `ControlClient` читає `CONTROL_BASIC_AUTH_USER` / `CONTROL_BASIC_AUTH_PASSWORD` з env; поки змінних нема на агентах, fallback на нинішнє значення з попередженням у лог. Креденшели прибрати з JSDoc.
2. `MAILPIT_QUICK_EMAILS_URL`: якщо змінної нема, попередження в лог про fallback на INI.
3. `.env.example`: нові змінні з порожніми значеннями.

Етап 3, з Максимом: змінні в `shared_dotenv` і на Jenkins-агентах; видалити fallback-и і значення з INI та коду (за зразком `requireMailPitUrl`); ротувати креденшели MailPit і Control; рішення, чи чистити git-історію.

## Готово, коли
Етап 1: `ControlClient` і MailPit quick-emails беруть значення з env, якщо воно задане; без змінних поведінка та сама і в лозі є попередження; `tsc` і lint 0.
Етап 3: секретів у робочому дереві 0; ротовано.

## Ризики
Етап 1 сам по собі безпеку не підвищує: значення лишаються в репо як fallback. Його цінність у тому, що етап 3 зводиться до "задати змінні, видалити fallback". Ротація без оновлення `shared_dotenv` зламає всі прогони.
