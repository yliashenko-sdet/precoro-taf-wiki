# T1-24. Гейт на гілках має справді перевіряти: валідація відповідей API
Біль: P1 (недовіра до результату), пункт 1 Head of QA. Найвища цінність у другому етапі, бо це якість гейта, а не додаткові тести.

## Контекст (виміряно 2026-09-22)
Смоук на гілці це `test_api.spec.ts`, близько 300 тестів у рантаймі; повний прогін їх не дублює. Отже якість цих тестів і є якість гілкового гейта.

Що вони перевіряють насправді:
- 116 із ~190 перевіряють `statusCode`; 94 викликають локальний `validateSchema`; 19 звіряються з базою; медіана assertions на тест 1.
- `validateSchema` (`test_api.spec.ts`, ~25 рядків) викликає перевірку **один раз для кореня і не рекурсує** у властивості: перевіряє лише, що корінь це об'єкт або масив і що присутні ключі з `required`.
- У всіх 60 схем у `src/config/test_data/Response_Schemas/` список `required` **порожній**.

Разом: гейт перевіряє статус-код і те, що відповідь є об'єктом. Перейменоване поле, змінений тип, зникле поле, неправильне значення проходять без сліду. Це пояснює 162 випадки "Smoke passed, but full run tests failed" із 218.

## Покриття (виміряно 2026-09-22)
| Метрика | Значення |
|---|---|
| Ендпоінтів у переліку Swagger | 507 |
| Методів клієнта з відомим шляхом | 249 |
| Методів, які викликає `test_api.spec.ts` | 101 |
| Унікальних ендпоінтів під тестами | 71 (14% від Swagger) |
| Найбільші діри | `approvalsteps` 0/16, `invoices` 3/47, `receipts` 2/28, `expenses` 2/28, `purchaseorders` 7/61, `budgets` 1/14 |

## Підхід
1. `ajv` замість рукописного валідатора; схеми генеруються з OpenAPI (`app.precorino.com/api/docs`), 60 рукописних схем видаляються.
2. Валідація вішається на рівень клієнта, а не в кожному тесті: кожна відповідь, яку TAF отримує, звіряється зі схемою свого ендпоінта. Тоді покриття валідацією дорівнює покриттю викликами.
3. Режим м'якого старту: перший тиждень розбіжності логуються і збираються в звіт, не валять тест. Інакше перший прогін стане червоним на всьому, бо схеми з OpenAPI суворіші за реальність.
4. Розбіжності розбираються за категоріями: помилка в OpenAPI, помилка продукту, застаріле очікування тесту. Перші дві йдуть у розробку, це і є цінність.
5. Поведінкові перевірки додаються за ризиком, починаючи з `approvalsteps` (0 із 16) і переходів життєвого циклу документів.

## Показники
- Ендпоінтів, які TAF викликає і які валідуються зі схемою: зараз частина від 71 → ціль 100% від тих, що викликаються.
- Ендпоінтів під поведінковими перевірками: 71 → за ризиком, не відсоток від Swagger (Swagger росте, цифра пливе).
- Кількість контрактних розбіжностей, знайдених за тиждень після вмикання: очікуємо ненульову, це доказ цінності.

## Готово, коли
Кожна відповідь, яку TAF отримує, валідується; рукописний валідатор і 60 схем видалені; знайдені розбіжності розібрані за категоріями.

## Залежності
Доступ до середовища для прогонів. Генерація типів і схем спільна з T3-06 і T3-07: робити разом, не двічі.
