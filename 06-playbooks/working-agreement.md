---
_organized: true
---
# Як працювати в репозиторії TAF

Для людини і для Claude Code однаково. Поля з `<...>` ще не заповнені.

## Де що

- Репозиторій TAF: `/Users/yevhenlyashenko/Work/src/precoro-e2e-playwright`; remote GitHub `avramch/precoro-e2e-playwright` (перевірено 2026-09-21). Гілки: `release` (аудит), `develop` (робоча, на 249 комітів попереду `release`), `fix/<дата>` гілки команди зливаються в `develop`.
- Ця вікі: `/Users/yevhenlyashenko/Work/Precoro/taf-wiki`, remote `https://github.com/yliashenko/precoro-taf-wiki` (приватний, гілка `main`). Поки не перенесена в репо TAF як `docs/taf-2.0/`; це станеться після апруву плану.
- Базова гілка для роботи: `<develop або назва гілки команди; підтвердити>`. Аудит зроблено на `release`; `develop` попереду (нові проекти po\_receive, matching, po\_from\_pr, spo), тому показники T0-13 перераховуються на `develop`.

## Гілки, коміти, PR

- Гілка на задачу: `taf2/T0-01-tsc-green`. Одна задача, один PR, не довше за день.
- Коміти англійською, коротко, з ID задачі: `T0-01: fix 3 tsc errors`.
- Codemod-и: окремий PR на кожен файл або групу файлів, щоб мержилось того ж дня.
- Push і PR: `<чи може Claude пушити сам | лише локальний коміт, push робить Yevhen>`.
- Ревʼю: спершу CodeRabbit у репозиторії, валідні коментарі обов'язково фіксити; далі людина: Олександр для команди документів, решта інфра-AQA.
- Зміни в Jenkinsfile робить інфра-AQA, але потрібен апрув від девів або Олександра.

## Локальний запуск

- Потрібні `.env.dev` з паролями і токенами, доступ до Precorino і MySQL. `<чи є у Claude доступ до середовища: так/ні>`.
- Без доступу можна робити: tsc, eslint, codemod-и з dry-run, контрольні показники, правки конфігу. Не можна: підтверджувати зміни прогоном.
- Перевірка змін у тестах: спершу зачеплені спеки локально, потім посилання на прогін у PR.

## Що Claude робить сам

- Задачі з `plan-to-tasks.md` за порядком із `00-status.md`, поки не впреться у "Заблоковано" або в рішення людини.
- Правки коду, локальні коміти, оновлення `00-status.md` і `99-log.md`, нові ADR у статусі Draft.
- Контрольні показники до і після кожної задачі, цифри в `metrics.md`.

## Де Claude зупиняється і питає

- Задача потребує Jenkins, секретів, доступу до середовища, яких нема.
- Зміна суперечить принципу з `02-principles/` або прийнятому ADR.
- Codemod зачепить понад ~20 файлів без можливості прогнати тести.
- Будь-що деструктивне для даних на Precorino.

## Протокол підходу
Ітеративність і перевірюваність важливіші за швидкість. Жоден підхід не починається без узгодженого контракту з чотирьох пунктів, і Claude Code формулює його першим повідомленням сесії:

1. **Фронт робіт**: що саме буде змінено, у яких файлах, чого не чіпаємо.
2. **Відповідність плану**: ID задачі з `plan-to-tasks.md`, картка в `tasks/`, біль із `pains.md`. Якщо робота не лягає в задачу, спершу з'являється задача або змінюється план, а не код.
3. **Одна задача за підхід**. Дві задачі в одному підході не беруться, навіть якщо друга "маленька".
4. **Умова зупинки**: що є результатом підходу, як його перевірити (показник, прогін, ревʼю), і на чому зупиняємось, навіть якщо можна йти далі.

Yevhen підтверджує контракт або править його. Робота починається після підтвердження. Наприкінці підходу: результат проти умови зупинки, розбіжності названо явно, статус і лог оновлено. Далі наступний контракт, не продовження за інерцією.

## Протокол сесії

**Початок:** прочитати `00-status.md`; якщо у задачі нема картки в `05-roadmap/tasks/`, спершу написати її і дати на перегляд, `02-principles/principles.md`, файл у `04-target/` для шару задачі, ADR задачі. Перевірити, що базова гілка актуальна.
**Кінець:** оновити `00-status.md` (зроблено, в роботі, наступна), рядок у `99-log.md`, показники в `metrics.md`, якщо змінились. Нове рішення → ADR Draft. Нове питання → `07-open-questions.md`.

## Мови

Код, коміти, PR: англійська. Вікі, ADR, обговорення: українська.
