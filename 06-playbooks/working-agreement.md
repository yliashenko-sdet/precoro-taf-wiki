# Як працювати в репозиторії TAF

Для людини і для Claude Code однаково. Поля з `<...>` ще не заповнені.

## Де що

- Репозиторій TAF: `/Users/yevhenlyashenko/Work/src/precoro-e2e-playwright` (Bitbucket).
- Ця вікі: `/Users/yevhenlyashenko/Work/Precoro/taf-wiki`, окремий git. Поки не перенесена в репо як `docs/taf-2.0/`.
- Базова гілка для роботи: `<release | назва робочої гілки команди>`. Аудит зроблено на `release`; робоча гілка команди попереду (нові проекти po\_receive, matching, po\_from\_pr, spo).

## Гілки, коміти, PR

- Гілка на задачу: `taf2/T0-01-tsc-green`. Одна задача, один PR, не довше за день.
- Коміти англійською, коротко, з ID задачі: `T0-01: fix 3 tsc errors`.
- Codemod-и: окремий PR на кожен файл або групу файлів, щоб мержилось того ж дня.
- Push і PR: `<чи може Claude пушити сам | лише локальний коміт, push робить Yevhen>`.
- Ревʼю: `<хто>`. CodeRabbit уже підключений.

## Локальний запуск

- Потрібні `.env.dev` з паролями і токенами, доступ до Precorino і MySQL. `<чи є у Claude доступ до середовища: так/ні>`.
- Без доступу можна робити: tsc, eslint, codemod-и з dry-run, контрольні лічильники, правки конфігу. Не можна: підтверджувати зміни прогоном.
- Перевірка змін у тестах: спершу зачеплені спеки локально, потім посилання на прогін у PR.

## Що Claude робить сам

- Задачі з `plan.md` за порядком із `00-status.md`, поки не впреться у "Заблоковано" або в рішення людини.
- Правки коду, локальні коміти, оновлення `00-status.md` і `99-log.md`, нові ADR у статусі Draft.
- Контрольні лічильники до і після кожної задачі, цифри в `metrics.md`.

## Де Claude зупиняється і питає

- Задача потребує Jenkins, секретів, доступу до середовища, яких нема.
- Зміна суперечить принципу з `02-principles/` або прийнятому ADR.
- Codemod зачепить понад ~20 файлів без можливості прогнати тести.
- Будь-що деструктивне для даних на Precorino.

## Протокол сесії

**Початок:** прочитати `00-status.md`, `02-principles/principles.md`, файл у `04-target/` для шару задачі, ADR задачі. Перевірити, що базова гілка актуальна.
**Кінець:** оновити `00-status.md` (зроблено, в роботі, наступна), рядок у `99-log.md`, лічильники в `metrics.md`, якщо змінились. Нове рішення → ADR Draft. Нове питання → `07-open-questions.md`.

## Мови

Код, коміти, PR: англійська. Вікі, ADR, обговорення: українська.
